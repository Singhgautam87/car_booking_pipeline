pipeline {
    agent any

    environment {
        COMPOSE_FILE    = "docker-compose.yml"
        COMPOSE_PROJECT = "car-booking-pipeline"
        SPARK_SUBMIT    = "docker exec spark-master /opt/spark/bin/spark-submit"
        SPARK_PACKAGES  = "org.apache.spark:spark-sql-kafka-0-10_2.12:3.4.2,org.postgresql:postgresql:42.5.0,mysql:mysql-connector-java:8.0.33,org.apache.hadoop:hadoop-aws:3.3.4,io.delta:delta-core_2.12:2.4.0"
        DELTA_CONF      = "--conf spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension --conf spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog"
        S3_CONF         = "--conf spark.hadoop.fs.s3a.endpoint=http://minio:9000 --conf spark.hadoop.fs.s3a.access.key=admin --conf spark.hadoop.fs.s3a.secret.key=admin123 --conf spark.hadoop.fs.s3a.path.style.access=true --conf spark.hadoop.fs.s3a.impl=org.apache.hadoop.fs.s3a.S3AFileSystem"
        SPARK_MEMORY    = "--driver-memory 2g --executor-memory 2g --conf spark.driver.maxResultSize=1g"
        ALERT_EMAIL     = "gautam901267@gmail.com"   
    }

    stages {

        stage('Checkout Code') {
            steps {
                checkout scm
                sh 'ls -la'
            }
        }

        stage('Cleanup') {
            steps {
                sh '''
                    echo "=== Full Cleanup ==="
                    docker rm -f postgres mysql kafka zookeeper minio kafka-ui \
                        spark-master great_expectations dashboard schema-registry || true
                    docker volume rm car-booking-pipeline_mysql_data || true
                    docker volume rm car-booking-pipeline_postgres_data || true
                    docker image prune -f || true
                    rm -rf /tmp/ge_* /tmp/spark_* /tmp/producer.py /tmp/car_booking.json || true
                    echo "✅ Cleanup complete!"
                '''
            }
        }

        stage('Start Containers') {
            steps {
                sh '''
                    docker-compose -p ${COMPOSE_PROJECT} up -d \
                        postgres mysql kafka zookeeper minio kafka-ui spark great_expectations

                    echo "Waiting for Kafka (60s)..."
                    sleep 60

                    for i in 1 2 3 4 5; do
                        if docker exec kafka kafka-broker-api-versions --bootstrap-server localhost:9092 2>/dev/null; then
                            echo "✅ Kafka ready!"
                            break
                        fi
                        sleep 10
                    done

                    # ✅ Files copy karo
                    docker cp spark/. spark-master:/opt/spark-apps/
                    docker exec -u root spark-master mkdir -p /opt/config
                    docker cp config/config.json spark-master:/opt/config/config.json
                    docker exec -u root great_expectations mkdir -p /opt/config
                    docker cp config/config.json great_expectations:/opt/config/config.json

                    docker-compose -p ${COMPOSE_PROJECT} ps
                    echo "✅ Containers ready!"
                '''
            }
        }

        stage('Start Schema Registry') {
            steps {
                sh '''
                    echo "=== Starting Schema Registry ==="
                    docker-compose -p ${COMPOSE_PROJECT} up -d schema-registry

                    for i in $(seq 1 12); do
                        if curl -s http://localhost:8085/subjects >/dev/null 2>&1; then
                            echo "✅ Schema Registry ready at http://localhost:8085"
                            break
                        fi
                        echo "Waiting... $i/12"
                        sleep 5
                    done
                '''
            }
        }

        stage('Initialize Databases') {
            steps {
                sh '''
                    echo "Waiting for PostgreSQL..."
                    for i in $(seq 1 30); do
                        if docker exec postgres pg_isready -U admin -d booking >/dev/null 2>&1; then
                            echo "✅ PostgreSQL ready!"
                            break
                        fi
                        sleep 2
                    done

                    echo "Waiting for MySQL..."
                    for i in $(seq 1 30); do
                        if docker exec mysql mysqladmin ping -u admin -padmin >/dev/null 2>&1; then
                            echo "✅ MySQL ready!"
                            break
                        fi
                        sleep 2
                    done

                    docker exec -i mysql mysql -u admin -padmin booking < sql/mysql.sql
                    docker exec -i postgres psql -U admin -d booking < sql/postgres.sql

                    echo "✅ All databases ready!"
                '''
            }
        }

        stage('Schema Registration') {
            steps {
                sh '''
                    echo "=== Registering Kafka Schema ==="
                    RUN_ID=$(date +%Y%m%d_%H%M%S)

                    # ✅ Schema Registry mein car-bookings schema register karo
                    RESPONSE=$(curl -s -o /tmp/schema_response.json -w "%{http_code}" \
                        -X POST http://localhost:8085/subjects/car-bookings-value/versions \
                        -H "Content-Type: application/vnd.schemaregistry.v1+json" \
                        -d '{
                            "schema": "{\"type\":\"record\",\"name\":\"CarBooking\",\"namespace\":\"com.carbooking\",\"fields\":[{\"name\":\"booking_id\",\"type\":\"string\"},{\"name\":\"customer_id\",\"type\":\"string\"},{\"name\":\"customer_name\",\"type\":\"string\"},{\"name\":\"car_id\",\"type\":\"string\"},{\"name\":\"model\",\"type\":\"string\"},{\"name\":\"category\",\"type\":\"string\"},{\"name\":\"pickup_location\",\"type\":\"string\"},{\"name\":\"drop_location\",\"type\":\"string\"},{\"name\":\"booking_date\",\"type\":\"string\"},{\"name\":\"return_date\",\"type\":\"string\"},{\"name\":\"price_per_day\",\"type\":\"double\"},{\"name\":\"rental_days\",\"type\":\"int\"},{\"name\":\"payment_amount\",\"type\":\"double\"},{\"name\":\"payment_method\",\"type\":\"string\"},{\"name\":\"payment_id\",\"type\":\"string\"},{\"name\":\"loyalty_tier\",\"type\":\"string\"},{\"name\":\"insurance_coverage\",\"type\":\"string\"},{\"name\":\"status\",\"type\":\"string\"},{\"name\":\"event_timestamp\",\"type\":\"string\"}]}"
                        }')

                    SCHEMA_ID=$(cat /tmp/schema_response.json | python3 -c \
                        "import json,sys; d=json.load(sys.stdin); print(d.get('id','1'))" 2>/dev/null || echo "1")

                    echo "Schema Response: $RESPONSE | Schema ID: $SCHEMA_ID"

                    # ✅ MySQL mein schema log karo
                    docker exec mysql mysql -u admin -padmin booking -e "
                        INSERT INTO schema_registry_log
                            (topic, schema_version, schema_id, change_type, schema_summary, is_compatible)
                        VALUES
                            ('car-bookings', 1, ${SCHEMA_ID}, 'REGISTERED',
                             'booking_id, customer_id, car_id, payment_amount, loyalty_tier + 14 more fields',
                             TRUE);
                    "

                    echo "=== Registered Subjects ==="
                    curl -s http://localhost:8085/subjects
                    echo "✅ Schema registered!"
                '''
            }
        }

        stage('Kafka Producer') {
            steps {
                sh '''
                    STAGE_START=$(date +%s)
                    RUN_ID=$(date +%Y%m%d_%H%M%S)

                    for i in 1 2 3 4 5; do
                        if docker exec kafka kafka-topics --create --if-not-exists \
                            --topic car-bookings \
                            --bootstrap-server localhost:9092 \
                            --partitions 3 \
                            --replication-factor 1 2>/dev/null; then
                            echo "✅ Topic created!"
                            break
                        fi
                        sleep 5
                    done

                    docker exec great_expectations pip install kafka-python requests --quiet

                    docker cp kafka/producer_with_schema_registry.py great_expectations:/tmp/producer.py
                    docker cp kafka/data/car_booking.json great_expectations:/tmp/car_booking.json
                    docker exec great_expectations python /tmp/producer.py

                    sleep 60

                    STAGE_END=$(date +%s)
                    DURATION=$((STAGE_END - STAGE_START))
                    docker exec mysql mysql -u admin -padmin booking -e "
                        INSERT INTO pipeline_run_stats
                            (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                        VALUES ('${RUN_ID}', 'kafka_producer', 1000, ${DURATION}, 'SUCCESS', NOW());
                    "
                    echo "✅ Producer complete | Duration: ${DURATION}s"
                '''
            }
        }

        stage('Setup MinIO Buckets') {
            steps {
                sh '''
                    docker exec minio sh -c "
                        rm -rf /data/delta &&
                        mkdir -p /data/delta/raw &&
                        mkdir -p /data/delta/transformed &&
                        mkdir -p /data/delta/curated
                    "
                    echo "✅ MinIO Delta Lake buckets ready!"
                '''
            }
        }

        stage('Spark Ingest Stream') {
            steps {
                sh 'docker exec -u root spark-master mkdir -p /home/spark/.ivy2/cache'
                sh 'docker exec -u root spark-master chown -R spark:spark /home/spark/.ivy2'
                retry(3) {
                    timeout(time: 15, unit: 'MINUTES') {
                        sh '''
                            STAGE_START=$(date +%s)
                            RUN_ID=$(date +%Y%m%d_%H%M%S)

                            $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                                /opt/spark-apps/jobs/ingest_stream.py
                            echo "✅ Bronze layer ready!"

                            STAGE_END=$(date +%s)
                            DURATION=$((STAGE_END - STAGE_START))
                            docker exec mysql mysql -u admin -padmin booking -e "
                                INSERT INTO pipeline_run_stats
                                    (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                                VALUES ('${RUN_ID}', 'spark_ingest', 0, ${DURATION}, 'SUCCESS', NOW());
                            "
                        '''
                    }
                }
            }
        }

        stage('Transform Data') {
            steps {
                timeout(time: 20, unit: 'MINUTES') {
                    sh '''
                        STAGE_START=$(date +%s)
                        RUN_ID=$(date +%Y%m%d_%H%M%S)

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/transform_customer.py
                        echo "✅ Customer done"

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/transform_booking.py
                        echo "✅ Booking done"

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/transform_cars.py
                        echo "✅ Cars done"

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/transform_payments.py
                        echo "✅ Payments done"

                        STAGE_END=$(date +%s)
                        DURATION=$((STAGE_END - STAGE_START))
                        docker exec mysql mysql -u admin -padmin booking -e "
                            INSERT INTO pipeline_run_stats
                                (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                            VALUES ('${RUN_ID}', 'spark_transform', 0, ${DURATION}, 'SUCCESS', NOW());
                        "
                        echo "✅ Silver layer ready!"
                    '''
                }
            }
        }

        stage('Merge Data') {
            steps {
                timeout(time: 15, unit: 'MINUTES') {
                    sh '''
                        STAGE_START=$(date +%s)
                        RUN_ID=$(date +%Y%m%d_%H%M%S)

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/merge.py

                        STAGE_END=$(date +%s)
                        DURATION=$((STAGE_END - STAGE_START))
                        docker exec mysql mysql -u admin -padmin booking -e "
                            INSERT INTO pipeline_run_stats
                                (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                            VALUES ('${RUN_ID}', 'spark_merge', 0, ${DURATION}, 'SUCCESS', NOW());
                        "
                        echo "✅ Curated layer ready!"
                    '''
                }
            }
        }

        stage('Write to PostgreSQL Staging') {
            steps {
                timeout(time: 15, unit: 'MINUTES') {
                    sh '''
                        STAGE_START=$(date +%s)
                        RUN_ID=$(date +%Y%m%d_%H%M%S)

                        $SPARK_SUBMIT $SPARK_MEMORY --packages $SPARK_PACKAGES $DELTA_CONF $S3_CONF \
                            /opt/spark-apps/jobs/write_to_postgres.py

                        RECORD_COUNT=$(docker exec postgres psql -U admin -d booking -t -c \
                            "SELECT COUNT(*) FROM customer_booking_staging;" | tr -d ' ')

                        STAGE_END=$(date +%s)
                        DURATION=$((STAGE_END - STAGE_START))
                        docker exec mysql mysql -u admin -padmin booking -e "
                            INSERT INTO pipeline_run_stats
                                (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                            VALUES ('${RUN_ID}', 'write_postgres', ${RECORD_COUNT}, ${DURATION}, 'SUCCESS', NOW());
                        "
                        echo "✅ PostgreSQL ready | Records: ${RECORD_COUNT}"
                    '''
                }
            }
        }

        stage('Data Validation') {
            steps {
                sh '''
                    STAGE_START=$(date +%s)
                    RUN_ID=$(date +%Y%m%d_%H%M%S)

                    docker exec great_expectations mkdir -p /ge/jobs
                    docker exec great_expectations mkdir -p /ge/great_expectations/expectations
                    docker exec great_expectations mkdir -p /ge/great_expectations/uncommitted/validations

                    docker cp great_expectations/jobs/run_ge.py great_expectations:/ge/jobs/run_ge.py
                    docker cp great_expectations/great_expectations/expectations/customer_booking_suite.json \
                        great_expectations:/ge/great_expectations/expectations/customer_booking_suite.json

                    docker exec great_expectations pip install mysql-connector-python psycopg2-binary sqlalchemy --quiet

                    docker exec mysql mysql -u admin -padmin booking -e "
                        DELETE FROM validation_results;
                        DELETE FROM validation_expectation_details;
                        DELETE FROM validation_failed_records;
                    "

                    docker exec great_expectations python /ge/jobs/run_ge.py

                    # ✅ DQ Score check karo
                    DQ_SCORE=$(docker exec mysql mysql -u admin -padmin booking -sN -e \
                        "SELECT COALESCE(success_rate, 0) FROM validation_results ORDER BY created_at DESC LIMIT 1;" \
                        | tr -d ' ')

                    echo "📊 Data Quality Score: ${DQ_SCORE}%"

                    FAILED_COUNT=$(docker exec mysql mysql -u admin -padmin booking -sN -e \
                        "SELECT COUNT(*) FROM validation_failed_records;" | tr -d ' ')

                    STAGE_END=$(date +%s)
                    DURATION=$((STAGE_END - STAGE_START))

                    docker exec mysql mysql -u admin -padmin booking -e "
                        INSERT INTO pipeline_run_stats
                            (run_id, stage_name, records_processed, duration_seconds, status, completed_at)
                        VALUES ('${RUN_ID}', 'data_validation', ${FAILED_COUNT}, ${DURATION}, 'SUCCESS', NOW());
                    "

                    # ✅ DQ Warning — 80% se kam hone par alert log karo
                    if [ ! -z "$DQ_SCORE" ]; then
                        docker exec mysql mysql -u admin -padmin booking -e "
                            INSERT INTO pipeline_alerts
                                (pipeline_name, stage_name, execution_date, error_message, alert_type)
                            SELECT 'car_booking_pipeline', 'data_validation', NOW(),
                                   CONCAT('DQ Score: ${DQ_SCORE}% — below 80% threshold'), 'DQ_WARNING'
                            WHERE ${DQ_SCORE} < 80;
                        "
                    fi

                    echo "✅ Validation complete!"
                '''
            }
        }

        stage('Start Dashboard') {
            steps {
                sh '''
                    docker rm -f dashboard || true
                    docker-compose -p ${COMPOSE_PROJECT} build --no-cache dashboard
                    docker-compose -p ${COMPOSE_PROJECT} up -d dashboard

                    sleep 15

                    for i in 1 2 3 4 5; do
                        if docker exec dashboard curl -s http://localhost:8050 >/dev/null 2>&1; then
                            echo "✅ Dashboard ready at http://localhost:8050"
                            break
                        fi
                        echo "Waiting... $i/5"
                        sleep 10
                    done
                '''
            }
        }
    }

    post {
        success {
            sh '''
                docker exec mysql mysql -u admin -padmin booking -e "
                    INSERT INTO pipeline_alerts
                        (pipeline_name, stage_name, execution_date, error_message, alert_type)
                    VALUES
                        ('car_booking_pipeline', 'all_stages', NOW(),
                         'Pipeline completed successfully', 'SUCCESS');
                " || true
            '''
            echo "✅ Pipeline complete!"
            echo "📊 Dashboard:       http://localhost:8050"
            echo "📋 Kafka UI:        http://localhost:8081"
            echo "🗄️  MinIO:           http://localhost:9001"
            echo "⚡ Spark UI:        http://localhost:8082"
            echo "🔀 Schema Registry: http://localhost:8085"

            // ✅ Success email
            mail(
                to:      "${ALERT_EMAIL}",
                subject: "✅ Car Booking Pipeline — SUCCESS #${BUILD_NUMBER}",
                body:    """
Pipeline completed successfully!

Build     : #${BUILD_NUMBER}
Duration  : ${currentBuild.durationString}
Branch    : ${GIT_BRANCH}

Links:
  Dashboard      : http://localhost:8050
  Kafka UI       : http://localhost:8081
  Schema Registry: http://localhost:8085
  Jenkins Build  : ${BUILD_URL}
                """.stripIndent()
            )
        }
        failure {
            sh '''
                docker exec mysql mysql -u admin -padmin booking -e "
                    INSERT INTO pipeline_alerts
                        (pipeline_name, stage_name, execution_date, error_message, alert_type)
                    VALUES
                        ('car_booking_pipeline', 'unknown', NOW(),
                         'Jenkins pipeline failed', 'FAILURE');
                " || true

                echo "=== Container Status ==="
                docker-compose -p ${COMPOSE_PROJECT} ps || true
                echo "=== Kafka Logs ==="
                docker logs kafka --tail 30 || true
                echo "=== Spark Logs ==="
                docker logs spark-master --tail 50 || true
                echo "=== Schema Registry Logs ==="
                docker logs schema-registry --tail 20 || true
                echo "=== Dashboard Logs ==="
                docker logs dashboard --tail 30 || true
            '''
            echo "❌ Pipeline failed!"

            // ✅ Failure email
            mail(
                to:      "${ALERT_EMAIL}",
                subject: "❌ Car Booking Pipeline — FAILED #${BUILD_NUMBER}",
                body:    """
Pipeline FAILED!

Build     : #${BUILD_NUMBER}
Stage     : ${STAGE_NAME}
Branch    : ${GIT_BRANCH}

Check Jenkins logs:
  ${BUILD_URL}console

Fix karo aur dobara push karo.
                """.stripIndent()
            )
        }
    }
}